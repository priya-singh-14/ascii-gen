# ascii-gen

Image GIFs and video convertor into ASCII charsets in the browser, with live controls for
character ramp, detail, tone, duotone color etc.

```
npm install
npm run dev      # http://localhost:5173
npm run build    # static bundle in dist/
```

## Browser support

Needs WebCodecs for video import and MP4/WebM export. Works best in Chrome.
